#include<stdio.h>
#include<conio.h>
main()
{
	int p,num,i,j;
	printf("enter number of elements you want:");
	scanf("%d",&p);
	printf("enter the number\n",p);
	i=1;
	while(i<=p)
	{
		scanf("%d",&num);
		if(i==1)
		j=num;
		i++;
	}
	printf("\n second highest %d",j);
	getch();
}
